<!DOCTYPE html>
<html>
  <head>
    <title>Erpoint</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= javascript_importmap_tags %>

    <!-- Inclua seu arquivo CSS personalizado aqui -->
    <%= stylesheet_link_tag "style" %>

    <%= javascript_include_tag 'application' %>

  </head>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <body>
    <header class="header">
      <div class="container-fluid">
        <div class="row">
          <div class="col">
            <h1>Dashboard</h1>
          </div>
          <div class="col text-end">
            <a href="#" class="btn btn-danger">Logout</a>
          </div>
        </div>
      </div>
    </header>
    
    <!-- Barra lateral -->
    <div class="sidebar">
      <ul class="nav flex-column">
        <h5>Categorias</h5>
        <li class="nav-item">
          <a class="nav-link" href="<%= categories_path %>">Categoria Index</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_category_path %>">Categoria New</a>
        </li>
      </ul>
      <hr/>
      <ul class="nav flex-column">
        <h5>Produtos</h5>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_produto_path %>">Produto New</a>
        </li>
      </ul>
      <hr/>
    </div>
    <hr>

    <div class="conteudo">
    <% if notice %>
        <div class="alert alert-success" role="alert">
            <%= notice %>
        </div>
    <% end %> 

    <% if alert %>
        <div class="alert alert-danger" role="alert">
            <%= alert %>
        </div>
    <% end %>
      <%= yield %>
    </div>  
  </body>
</html>
